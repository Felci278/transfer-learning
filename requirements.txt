# ── Urdu Sentiment Analysis — Python dependencies ─────────────────────────────
# NOTE: torch is excluded here.
# The Dockerfile installs the CPU-only wheel separately:
#   pip install torch==2.4.1+cpu --index-url https://download.pytorch.org/whl/cpu
# For local development (with GPU), install manually:
#   pip install torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1
# ───────────────────────────────────────────────────────────────────────────────

# HuggingFace stack (pinned to match notebook training environment)
transformers==4.41.2
datasets==2.19.1
peft==0.11.1
accelerate==0.31.0
sentencepiece==0.2.0
tokenizers>=0.19.0

# Numerics
numpy==1.26.4
scikit-learn==1.4.2

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic>=2.0

# Utilities
tqdm
huggingface-hub>=0.23.0
safetensors>=0.4.0
